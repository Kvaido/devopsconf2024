{{- if .Values.logging.enabled }}
{{- if $.Values.logging.vector.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ include "ns-go.fullname" . }}-logging-tls"
  labels:
    {{- include "ns-go.main.labels" . | nindent 4 }}
  annotations:
    {{- include "ns-go.main.annotations" . | nindent 4 }}
type: Opaque
stringData:
...
{{- end }}
{{- end }}
